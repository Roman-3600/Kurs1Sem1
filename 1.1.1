#include <stdio.h>
#include <math.h>

int main() {
    // Исходные чила
    double x = 1.426;
    double y = -1.22;
    double z = 3.5;

    // Вычисление переменной a
    // a = 2*cos(x - pi/6) / (1/2 + sin^2(y))
    double a_top = 2 * cos(x - M_PI / 6);
    double a_bottom = 0.5 + pow(sin(y), 2);
    double a = a_top / a_bottom;

    // Вычисление переменной b
    // b = 1 + z^2 / (3 + z^2/5)
    double b_top = pow(z, 2);
    double b_bottom = 3 + pow(z, 2) / 5;
    double b = 1 + b_top / b_bottom;

    // Вывод исходных значений
    printf("Исходные значения:\n");
    printf("x = %.3f\n", x);
    printf("y = %.2f\n", y);
    printf("z = %.1f\n", z);

    // Вывод результатов
    printf("\nРезультаты:\n");
    printf("a = %.8f\n", a);
    printf("b = %.8f\n", b);

    return 0;
}
